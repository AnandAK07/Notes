import React from 'react'

const ShowScore = ({score}) => {
  console.log(score)
  return (
    <div>
      {/* <h1>Score Board: {score}</h1> parent to child props*/}
    </div>
  )
}

export default ShowScore