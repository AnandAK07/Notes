import React from 'react'

export const ProductCard = ({description, category, image, title, price, rating}) => {
    return (
        <div>
        {/* {data?.map((item) => {
        })} */}
        </div>
    )
}
